{
  "name": "hello-ios-example-stub",
  "private": true,
  "description": "Broker/parity iOS demos live in tish-desktop/examples/hello-ios — this crate is host-only",
  "scripts": {
    "build": "echo 'Use tish-desktop/examples/hello-ios (npm run example:hello-ios)' && exit 1",
    "run": "echo 'Use tish-desktop/examples/hello-ios (npm run example:hello-ios)' && exit 1",
    "dev": "echo 'Use tish-desktop/examples/hello-ios (npm run example:hello-ios)' && exit 1"
  }
}
