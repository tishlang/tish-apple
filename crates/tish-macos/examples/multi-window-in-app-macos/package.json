{
  "name": "multi-window-in-app-macos-example",
  "private": true,
  "version": "0.0.0",
  "description": "macos.openWindow — second NSWindow in the same process (one Dock icon)",
  "dependencies": {
    "tish-macos": "file:../.."
  },
  "scripts": {
    "build": "cargo run --manifest-path ../../../tish/Cargo.toml -q -p tishlang -- build --target native --native-backend rust --feature full src/main.tish -o dist/multi-window-in-app-macos",
    "dev": "npm run build && ./dist/multi-window-in-app-macos",
    "run": "npm run dev",
    "start": "npm run dev"
  }
}
