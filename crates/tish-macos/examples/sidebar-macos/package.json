{
  "name": "sidebar-macos-example",
  "private": true,
  "version": "0.0.0",
  "description": "NSSplitViewController sidebar + SF Symbols + standard toolbar toggle",
  "dependencies": {
    "tish-macos": "file:../.."
  },
  "scripts": {
    "build": "cargo run --manifest-path ../../../tish/Cargo.toml -q -p tishlang -- build --target native --native-backend rust src/main.tish -o dist/sidebar-macos",
    "dev": "npm run build && ./dist/sidebar-macos",
    "run": "npm run dev",
    "start": "npm run dev"
  }
}
