{
  "name": "ai-messenger-macos",
  "private": true,
  "type": "module",
  "description": "AIM-style multi-window AI messenger mock (native markdown, prefs, snap)",
  "dependencies": {
    "@tishlang/tish": "^2.39.0",
    "@tishlang/tish-macos": "^0.2.0"
  },
  "scripts": {
    "build": "tish build --target native --native-backend rust --feature full src/main.tish -o dist/ai-messenger-macos",
    "dev": "npm run build && ./dist/ai-messenger-macos",
    "run": "npm run dev",
    "start": "npm run dev"
  }
}
