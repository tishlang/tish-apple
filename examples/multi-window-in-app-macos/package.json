{
  "name": "multi-window-in-app-macos",
  "private": true,
  "type": "module",
  "description": "macos.openWindow — second NSWindow in the same process (one Dock icon)",
  "dependencies": {
    "@tishlang/tish": "^2.39.0",
    "@tishlang/tish-macos": "^0.2.0"
  },
  "scripts": {
    "build": "tish build --target native --native-backend rust --feature full src/main.tish -o dist/multi-window-in-app-macos",
    "dev": "npm run build && ./dist/multi-window-in-app-macos",
    "run": "npm run dev",
    "start": "npm run dev"
  }
}
