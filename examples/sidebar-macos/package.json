{
  "name": "sidebar-macos",
  "private": true,
  "type": "module",
  "description": "NSSplitViewController sidebar + SF Symbols + standard toolbar toggle",
  "dependencies": {
    "@tishlang/tish": "^2.39.0",
    "@tishlang/tish-macos": "^0.2.0"
  },
  "scripts": {
    "build": "tish build --target native --native-backend rust src/main.tish -o dist/sidebar-macos",
    "dev": "npm run build && ./dist/sidebar-macos",
    "run": "npm run dev",
    "start": "npm run dev"
  }
}
